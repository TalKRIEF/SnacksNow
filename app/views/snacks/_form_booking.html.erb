<%= simple_form_for [snack, booking] do |f|%>
  <%= f.input :date,
              as: :string,
              input_html: { data: { controller: "datepicker" } } %>
  <%= f.submit class: "button-light-red", value: "Confirmer la réservation" %>
<% end %>
